[Unit]
Description=Wargear
Requires=docker.service
After=docker.service network-online.target

[Service]
WorkingDirectory=/home/ubuntu/warband-creator
ExecStart=/usr/bin/docker compose -p wargear -f docker-compose.yml -f docker-compose.prod.yml up
ExecStop=/usr/bin/docker compose -p wargear -f docker-compose.yml -f docker-compose.prod.yml down
Restart=always

[Install]
WantedBy=multi-user.target
